name: Calling Reusable Workflows
on:
    workflow_dispatch:
        inputs:
            name:
                description: 'Input description'
                type: string
                default: 'Ali'
                required: false

jobs:
    calling-reusable-workflow:
        uses: github-actions-course-jjsierra/workflows/.github/workflows/reusable.yaml@b1a7925
        with:
            name: ${{ inputs.name }}
        secrets:
            access-token: ${{ secrets.ACCESS_TOKEN }}